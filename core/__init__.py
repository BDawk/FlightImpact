"""FlightImpact core — algorithms, models, and API contract.

This package contains everything that ships in both the dev kit and the
production unit. It must never import from `devkit/` or `app/`.
"""

__version__ = "0.1.0"
