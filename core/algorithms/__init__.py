"""Core algorithms — CV, DSP, fusion, ballistics.

Each subpackage has a clean public interface defined in its __init__.
Implementations are stubs at scaffold time; they get filled in as we
build each phase of the dev kit.
"""
