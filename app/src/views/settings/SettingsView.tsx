// Settings moved into the gear-icon modal (SettingsModal.tsx) — no longer a standalone route.
// This stub prevents stale imports from breaking the build.
export function SettingsView() { return null; }
